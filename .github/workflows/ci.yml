name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  formal-api:
    name: Formal API domain tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install formal API dependencies
        run: python -m pip install -e "apps/api[dev]"

      - name: Run formal API domain tests
        run: PYTHONPATH=apps/api python -m pytest apps/api/tests

  formal-web:
    name: Formal web build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web-v4
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/web-v4/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm run test

      - name: Build formal web
        run: npm run build
